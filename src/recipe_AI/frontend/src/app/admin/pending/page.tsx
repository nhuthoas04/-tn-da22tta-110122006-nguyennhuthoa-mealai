export const dynamic = 'force-dynamic';
export const revalidate = 0;

import AdminPendingClient from './AdminPendingClient';

export default function AdminPendingPage() {
  return <AdminPendingClient />;
}
